{
  "name": "applicationbase-e2e",
  "version": "1.0.0",
  "description": "E2E tests com Playwright para fluxos críticos (User CRUD, Auth)",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:auth": "playwright test auth.spec.ts",
    "test:user": "playwright test user.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1"
  }
}
